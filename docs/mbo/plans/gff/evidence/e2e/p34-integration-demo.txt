# p3+p4 combined integration demo — end to end
# date UTC: 2026-07-26T04:44:10Z
# world: scratch HOME (mktemp); real $HOME untouched; binary built from the p34-tui-gen branch

# gff p3+p4 integration demo — 2026-07-26T04:36:57Z
# world: scratch HOME under /tmp/gff-p34-demo.c5EKhi (real $HOME untouched)

### 0. build the combined binary (TUI + gen) from the p34-tui-gen branch

$ gff version
gff vdev
  Commit:      none
  Dirty:       false
  Build Date:  unknown
  Description: gff — git fast features: layered feature flags persisted in git

### 1. scaffold a demo repo with 1 bool + 1 radio choice + 1 checkbox choice

$ gff lint (exit $?)
lint exit: 0

### 2. install (register namespace + snapshot), then list from a foreign CWD

$ gff install
installed com.example.quickstart (quickstart)

$ gff list --source com.example.quickstart   # from $HOME, via the snapshot
PATH                TYPE    VALUE         LAYER          DESCRIPTION
demo.pkg.manager    choice  auto          user-snapshot  Package manager selection
demo.shell.plugins  choice  fzf,starship  user-snapshot  Optional shell plugins
demo.ui.dashboard   bool    true          user-snapshot  Render the dashboard pane

### 3. flip the bool off via the CLI writer (user override, 0600)

$ gff set demo.ui.dashboard false && gff list demo.ui.*
PATH               TYPE  VALUE  LAYER          DESCRIPTION
demo.ui.dashboard  bool  false  user-override  Render the dashboard pane

### 4. gff gen — typed accessors generated from the SAME world (p4)

$ gff gen --pkg demoflags --out ${DEMO}/genout

$ head -30 genout/demoflags.go
// Code generated by gff gen; DO NOT EDIT.
package demoflags

import (
	"github.com/sfc-gh-eraigosa/dotfiles/sdk/gff/pkg/gff"
)

// BoolFlag is a typed accessor for a boolean feature flag.
// Bool() delegates to pkg/gff.Bool with the literal key embedded at code-gen time.
type BoolFlag struct{ key string }

// Bool returns the effective boolean value of the flag across all 5 resolver layers.
func (f BoolFlag) Bool() (bool, error) { return gff.Bool(f.key) }

// ChoiceFlag is a typed accessor for a choice (single- or multi-select) feature flag.
// All methods delegate to pkg/gff with the literal key embedded at code-gen time.
type ChoiceFlag struct{ key string }

// Selected returns the effective selected option ids.
func (f ChoiceFlag) Selected() ([]string, error) { return gff.Selected(f.key) }

// IsSelected reports whether optionID is currently selected.
func (f ChoiceFlag) IsSelected(optionID string) (bool, error) { return gff.IsSelected(f.key, optionID) }

// StringValues returns the string payloads of the currently-selected options.
func (f ChoiceFlag) StringValues() ([]string, error) { return gff.StringValues(f.key) }

// IntValues returns the int64 payloads of the currently-selected options.
func (f ChoiceFlag) IntValues() ([]int64, error) { return gff.IntValues(f.key) }


### 5. compile + RUN the generated accessors against the live world

$ go run .   # from inside the demo repo, scratch HOME
demo.ui.dashboard (via generated accessor): false <nil>
demo.pkg.manager selected ids: [auto] <nil>
demo.shell.plugins typed values: [fzf starship] <nil>
^ dashboard=false proves gen accessors read the SAME user override gff set wrote

### 6. export — the injection-safe bridge (shell + dotenv shown)

$ gff export --format shell
GFF_DEMO_PKG_MANAGER=auto
GFF_DEMO_SHELL_PLUGINS=fzf,starship
GFF_DEMO_UI_DASHBOARD=false

$ gff export --format dotenv -o .env && cat .env
GFF_DEMO_PKG_MANAGER=auto
GFF_DEMO_SHELL_PLUGINS=fzf,starship
GFF_DEMO_UI_DASHBOARD=false

### scratch world preserved for the TUI segment: /tmp/gff-p34-demo.c5EKhi
DEMO_DIR=/tmp/gff-p34-demo.c5EKhi

############################################
## PART 2 — live TUI segment (tmux-driven, real binary, real TTY)
############################################

=== TUI FRAME 1 — initial view (areas collapsed) ===
> ▶ demo
↑/↓ navigate  Enter expand/collapse  Space toggle  q quit

=== TUI FRAME 2 — Enter expands area 'demo' (provenance column; dashboard=false via CLI override) ===
> ▼ demo
  demo.pkg.manager                          auto  default  repo-live  Package manager selection
  demo.shell.plugins                        fzf,starship  default  repo-live  Optional shell plugins
  demo.ui.dashboard                         false  override  user-override  Render the dashboard pane
↑/↓ navigate  Enter expand/collapse  Space toggle  q quit

=== TUI FRAME 3 — Space on demo.ui.dashboard: bool toggled false->true, layer=user-override ===
  ▼ demo
  demo.pkg.manager                          auto  default  repo-live  Package manager selection
  demo.shell.plugins                        fzf,starship  default  repo-live  Optional shell plugins
>   demo.ui.dashboard                         true    override   user-override  Render the dashboard pane
↑/↓ navigate  Enter expand/collapse  Space toggle  q quit

=== TUI FRAME 4 — Space on demo.pkg.manager: radio picker opens (typed values shown) ===
Pick option for: demo.pkg.manager (radio (single))
> (●)  auto              Auto-detect                     auto
  ( )  apt               Debian/Ubuntu apt               apt
  ( )  brew              Homebrew                        brew
↑/↓ navigate  Enter select  Esc cancel

=== TUI FRAME 5 — Down+Enter in radio picker: 'apt' selected + confirmed, back in list ===
  ▼ demo
>   demo.pkg.manager                          apt     override   user-override  Package manager selection
  demo.shell.plugins                        fzf,starship  default  repo-live  Optional shell plugins
  demo.ui.dashboard                         true  override  user-override  Render the dashboard pane
↑/↓ navigate  Enter expand/collapse  Space toggle  q quit

=== CLI verification after quitting the TUI (same scratch world) ===
$ gff get demo.ui.dashboard
true
$ gff get demo.pkg.manager
apt
$ cat ~/.config/gff/config.yaml (scratch HOME)
demo.pkg.manager: apt
demo.ui.dashboard: true
