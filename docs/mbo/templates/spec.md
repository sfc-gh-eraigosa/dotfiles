# <objective> — spec

- **Slug:** <slug>
- **Date:** <YYYY-MM-DD>
- **Status:** Draft | Approved
- **Relates to:** issue #<n> / PR #<n> / design `../designs/<slug>.md`

## 1. Goal
One paragraph: what the user gets.

## 2. Use cases
Each as **actor / trigger / flow / acceptance criteria**.

## 3. Architecture
Components & boundaries (each independently testable); data flow; interfaces.

## 4. Behavior / features
The concrete capabilities.

## 5. Evaluation criteria (per feature)
For each feature: trigger predicate · fires · must-not-fire · edge · pass. (Make every rule a test.)

## 6. Verification harness
How it's tested with high confidence — automated layers + any human-evidenced gates; coverage.

## 7. Prerequisites / dependencies

## 8. Out of scope (and why)

## 9. Rollback

> Produced via `superpowers:brainstorming`. The matching plan goes in `../plans/<slug>.md`.
> Register / update `../index.md`.
