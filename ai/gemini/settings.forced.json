{
  "_comment": "FORCED (immutable) Gemini settings subset. install_gemini_skills.sh deep-merges this over the host-owned ~/.gemini/settings.json on every run (see opt/scripts/system/apply-forced-settings.sh). Hooks reference well-known $HOME paths populated by copy, NOT the per-project Gemini project-dir variable (which resolves to the active project root and so would only fire inside the dotfiles repo). See docs/mbo/designs/2026-06-02-ai-config-home-provisioning.md, D4. Note: _comment keys are stripped before the merge, so this never reaches the live settings file.",
  "hooks": {
    "BeforeTool": [
      {
        "matcher": "run_shell_command",
        "hooks": [
          {
            "name": "safety-guard",
            "type": "command",
            "command": "$HOME/.gemini/hooks/safety_guard.sh"
          }
        ]
      },
      {
        "matcher": "write_file|replace|run_shell_command",
        "hooks": [
          {
            "name": "privacy-guard",
            "type": "command",
            "command": "$HOME/.gemini/hooks/privacy_guard.sh"
          }
        ]
      }
    ]
  }
}
