# Google Workspace CLI Configuration (ai/gws)

This directory contains configuration templates and documentation for the Google Workspace CLI (`gws`).

## Configuration

- `config.json.template`: Template for the `gws` configuration file.
- `client_secret.json.template`: Template for OAuth client secrets.

## Usage

Run `google-cli-setup.sh` to initialize the configuration and install the necessary tools.
